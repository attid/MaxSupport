# Кнопка "Другой вопрос" — закрытие и пересоздание тикета

## Контекст
Кнопка "Другой вопрос" делала то же самое что "Закрыть" — просто закрывала тикет без создания нового.

## План изменений
1. [x] `handle_another_question` — закрывает текущий тикет без уведомления клиента
2. [x] Отправляет клиенту "Ваш вопрос переведён в новое обращение"
3. [x] Создаёт новый тикет из последнего сообщения клиента (`force_new_ticket=True`)
4. [x] Добавлен параметр `notify_client` в `close_ticket`

## Коммиты
- `7a5e10c` feat: implement "another question" — close ticket and reopen from last message
